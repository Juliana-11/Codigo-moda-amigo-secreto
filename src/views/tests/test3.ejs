<!DOCTYPE html>
<html lang="en">
    <%-include('../partials/head', {title: "home"})%> 
<body>
    <%-include('../partials/header')%> 

    <!--Prueba de funcionamiento del modelo group member-->
    <p>Integrantes de grupos:</p>
    <% for( let i = 0; i < member.length; i++ ) { %>
        <%= member[i].idGroupMember %>
    <% } %>

    <%-include('../partials/footer')%> 
</body>
</html>