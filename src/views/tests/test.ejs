<!DOCTYPE html>
<html lang="en">
    <%-include('../partials/head', {title: "home"})%> 
<body>
    <%-include('../partials/header')%> 

    <!--Prueba de funcionamiento del modelo user-->
    <p>Users:</p>
    <% for( let i = 0; i < users.length; i++ ) { %>
        <%= users[i].name %>
    <% } %>

    <%-include('../partials/footer')%> 
</body>
</html>